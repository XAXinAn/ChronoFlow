# 🚀 ChronoFlow（时纪流）新手部署指南

> **写给你（一个自称"傻子"但实际上很聪明的人）的话：**
> 这份文档假设你**什么都不懂**。每一步都会写清楚"在哪里点、输入什么、为什么这样做"。
> 如果遇到不懂的名词，不要慌，后面基本都附了解释。照着做就行。

---

## 📖 目录

1. [这个项目是什么](#1-这个项目是什么)
2. [你的电脑上已经装了什么](#2-你的电脑上已经装了什么)
3. [启动数据库服务（Docker）](#3-启动数据库服务)
4. [在 IDEA 中启动后端](#4-在-idea-中启动后端)
5. [在 Android Studio 中启动前端 App](#5-在-android-studio-中启动前端-app)
6. [访问管理后台（网页版）](#6-访问管理后台网页版)
7. [日常开发常用操作](#7-日常开发常用操作)
8. [常见问题排查](#8-常见问题排查)
9. [项目结构速查](#9-项目结构速查)

---

## 1. 这个项目是什么

时纪流是一个**日程管理 App**。用户拍一张通知的截图，AI 自动识别文字并创建日程。

项目由三部分组成：

| 部分 | 是什么 | 用什么写的 | 在哪里 |
|------|--------|-----------|--------|
| **后端** | 处理数据、提供接口的"大脑" | Java（Spring Boot） | `backend/` 文件夹 |
| **前端 App** | 用户手机上用的 App | Flutter | `frontend/` 文件夹 |
| **管理后台** | 管理员在电脑浏览器里用的网页 | React | `web/` 文件夹 |

它们的关系：
```
手机 App  ──→  后端（Java）  ──→  MySQL（存数据）
                                   Redis（缓存）
                                   MinIO（存图片）
浏览器 ──→  管理后台（React 网页）  ──→  后端（同一个）
```

---

## 2. 你的电脑上已经装了什么

以下软件**已经装好**，不需要再装：

| 软件 | 位置 | 作用 |
|------|------|------|
| **Java 17** | `D:\IDEA\java\jdk-17.0.1` | 运行后端 |
| **Java 21** | `D:\Android\Android Studio\jbr` | 编译 Flutter App |
| **Docker** | 系统自带 | 运行 MySQL、Redis、MinIO |
| **Flutter** | `D:\Flutter\flutter_windows_3.44.1-stable\flutter` | 开发 App |
| **Node.js** | 系统自带 | 构建管理后台 |
| **IntelliJ IDEA** | 你已有的 | 写后端代码 |
| **Android Studio** | `D:\Android\Android Studio` | 写前端代码、运行模拟器 |
| **Android SDK** | `D:\Android\SDK` | 编译 Android App |

---

## 3. 启动数据库服务

> **这是在干什么？** 启动 MySQL（存数据）、Redis（缓存）、MinIO（存图片）。它们运行在 Docker 容器里，类似"虚拟机里的迷你系统"。

### 步骤

**① 打开终端（命令行）**

按键盘 `Win + R`，输入 `bash`，回车。会出现一个黑色窗口。

**② 进入后端目录**

在黑色窗口里输入以下命令，每输完一行按回车：

```bash
cd D:/Android/project/ChronoFlow-develop/backend
```

**③ 一键启动所有服务**

```bash
docker compose up -d
```

你会看到类似这样的输出：
```
 Container ChronoFlow-mysql Running
 Container ChronoFlow-minio Running
 Container ChronoFlow-redis Running
```

**④ 验证是否启动成功**

```bash
docker ps --filter "name=ChronoFlow"
```

如果看到三个容器状态都是 `(healthy)`，说明成功了。

### 常用命令

```bash
# 查看服务状态
docker ps --filter "name=ChronoFlow"

# 停止服务（数据保留，下次启动数据还在）
docker compose down

# 停止并删除所有数据（相当于恢复出厂设置）
docker compose down -v
```

> ⚠️ **重要**：每次重启电脑后，Docker 容器可能不会自动启动。如果后端报"连不上数据库"，先执行一次 `docker compose up -d`。

---

## 4. 在 IDEA 中启动后端

> **这是在干什么？** 启动 Java 后端程序，让它监听 `localhost:8080`，等待 App 和管理后台发请求过来。

### 4.1 安装 EnvFile 插件（只需一次）

这个插件让 IDEA 能读取 `.env` 文件里的配置（数据库密码、密钥等）。

1. 打开 IntelliJ IDEA
2. 点击菜单栏 `File` → `Settings`（或按 `Ctrl + Alt + S`）
3. 左侧选 `Plugins`
4. 顶部搜索框输入 `EnvFile`
5. 找到 **EnvFile** 插件 → 点击 `Install`
6. 安装完后点 `Restart IDE` 重启 IDEA

### 4.2 首次打开项目

1. 打开 IDEA
2. 点击 `Open`（或 File → Open）
3. 选择目录：`D:\Android\project\ChronoFlow-develop\backend`
4. 点 `OK`
5. IDEA 右下角会显示 "Maven importing..." 或 "Loading project..."，等待完成（可能需要几分钟下载依赖）

### 4.3 配置并启动

1. 在 IDEA 左侧文件树找到：
   ```
   backend → src → main → java → com.chronoflow.backend → BackendApplication
   ```

2. 双击打开 `BackendApplication.java`

3. 你会看到代码第 12 行左右有一个 `main` 方法，左边有一个**绿色小三角 ▶️**

4. **点击绿色小三角** → 选择第一个 `Run 'BackendApplication'`

5. 第一次运行会报错，别慌！这是正常的。我们需要添加环境变量配置：
   - 点击 IDEA 右上角的运行配置下拉框（绿色三角旁边的下拉菜单）
   - 选择 `Edit Configurations...`
   - 在右侧找到 `Environment variables` 这一栏
   - 点击这一栏右侧的**文件夹图标** 📁
   - 在弹出的窗口中点 `+`（加号）
   - 选择 `D:\Android\project\ChronoFlow-develop\backend\.env`
   - 点 `OK` → 再点 `OK`

6. **再次点击绿色小三角 ▶️** 启动

7. IDEA 底部会出现 `Run` 窗口，等待看到这行字：
   ```
   Started BackendApplication in X.XXX seconds
   ```
   说明后端启动成功了！

### 4.4 验证后端是否正常

打开浏览器，访问：
```
http://localhost:8080/api/app/version
```

如果看到类似 `{"version":"1.1.0","versionCode":1}` 的 JSON 文字，说明后端正常运行。

### 4.5 停止后端

在 IDEA 底部 Run 窗口，点左边**红色方块 ⬛** 即可停止。

---

## 5. 在 Android Studio 中启动前端 App

> **这是在干什么？** 启动 Flutter App，在模拟器上运行，可以通过 App 注册/登录/创建日程。

### 5.1 打开项目

1. 打开 **Android Studio**
2. 点击 `Open`（或 File → Open）
3. 选择目录：`D:\Android\project\ChronoFlow-develop\frontend`
4. 点 `OK`
5. 等待右下角进度条跑完（"Syncing..."、"Indexing..." 等，可能需要几分钟）

### 5.2 启动 Android 模拟器

如果你的模拟器已经开着，跳过这一步。

**方法一：用命令行**
```bash
flutter emulators --launch Medium_Phone
```

**方法二：用 Android Studio**
1. 点击 Android Studio 右上角的设备下拉框（显示 "No Devices" 或设备名的地方）
2. 选择 `Device Manager`
3. 找到 `Medium Phone`（或 `testphone`），点右边的**绿色三角 ▶️**
4. 等待模拟器窗口出现，直到看到手机桌面

### 5.3 运行 App

**方式一：在 Android Studio 中直接点运行**

1. 确保右上角设备下拉框已选择模拟器（如 `Medium Phone`）
2. 点击绿色三角 ▶️ `Run`
3. 在 Run Configuration 弹窗中，找到 `Additional run args` 字段
4. 输入：`--dart-define=BASE_URL=http://10.0.2.2:8080/api`
   > 解释：`10.0.2.2` 是 Android 模拟器访问电脑的专用地址，相当于"电脑的 localhost"
5. 点击 `Run`
6. 等待编译（首次约 3-5 分钟），编译完成后 App 会自动安装到模拟器并打开

**方式二：命令行启动**

```bash
cd D:/Android/project/ChronoFlow-develop/frontend
flutter run --dart-define=BASE_URL=http://10.0.2.2:8080/api
```

### 5.4 App 功能一览

启动后你会看到：

- **主页**：日历 + "拍照识别"和"相册上传"入口
- **群组**（底部第二个标签）：加入/创建群组
- **个人中心**（底部第三个标签）：个人信息、意见反馈

> 📌 **注意**：注册需要阿里云实人认证（人脸识别），如果你没有配置好阿里云服务，注册可能会失败。如果需要在模拟器中测试，可以先从数据库里手动创建测试用户。

### 5.5 Hot Reload（热重载）

修改代码后，不需要重新编译整个 App：
- 在 App 运行时，修改 Dart 代码
- 按 `r` 键 → 热重载（保留当前状态，快速更新 UI）
- 按 `R` 键 → 热重启（重置状态，完全重启 App）
- 按 `q` 键 → 退出

---

## 6. 访问管理后台（网页版）

> **这是在干什么？** 打开一个网页，用管理员身份管理用户、群组、日程、反馈。

### 6.1 打开管理后台

1. 确保后端已经在运行（参考第 4 节）
2. 打开浏览器（Chrome/Edge 都可以）
3. 地址栏输入：**`http://localhost:8080/admin/`**
4. 回车

### 6.2 登录

- **用户名**：`admin`
- **密码**：`admin123`

> ⚠️ 登录后建议立即修改密码！（目前只能在数据库里手动改）

### 6.3 管理后台能做什么

| 菜单 | 功能 |
|------|------|
| **反馈管理** | 查看用户提交的意见反馈，回复、修改状态 |
| **用户管理** | 查看所有注册用户 |
| **群组管理** | 查看/编辑/删除群组 |
| **日程管理** | 新建/编辑/删除日程 |

### 6.4 管理后台怎么更新的

管理后台是 React 写的，源码在 `web/` 文件夹。修改代码后需要重新构建：

```bash
cd D:/Android/project/ChronoFlow-develop/web
npm install          # 首次或新增依赖时
npm run build        # 构建，自动输出到后端静态目录
```

构建完成后**重启后端**即可看到更新。

---

## 7. 日常开发常用操作

### 日常启动流程

每次开始写代码时，按这个顺序操作：

```
1. 启动 Docker 服务 → docker compose up -d
2. 启动后端 → IDEA 点 Run
3. 启动模拟器 → 打开 Android 模拟器
4. 启动 App → Android Studio 点 Run

（管理后台不需要"启动"，它已经在后端里了，直接用浏览器访问即可）
```

### 真机调试（用真实手机测试）

如果你想把 App 装到手机上调试：

1. 手机开启**开发者模式**和 **USB 调试**
2. 用 USB 线连接电脑
3. 在手机上允许 USB 调试
4. 查看电脑的局域网 IP：
   - 按 `Win + R` → 输入 `cmd` → 输入 `ipconfig`
   - 找到 "IPv4 地址"，类似 `192.168.x.x`
5. 运行 App 时指定你电脑的 IP：
   ```bash
   flutter run --dart-define=BASE_URL=http://192.168.x.x:8080/api
   ```
6. 确保手机和电脑在**同一个 WiFi** 下

### 数据库操作

**用命令行查看数据库：**
```bash
docker exec -it ChronoFlow-mysql mysql -uroot -p123456 chronoflow
```
进去后可以执行 SQL，例如 `SELECT * FROM users;`，退出输入 `exit`。

**用图形界面工具连接：**
- 主机：`localhost`
- 端口：`3307`
- 用户名：`root`
- 密码：`123456`
- 数据库：`chronoflow`

推荐工具：[DBeaver](https://dbeaver.io/)（免费）或 IDEA 自带的 Database 工具。

### MinIO 管理台

- 地址：`http://localhost:9001`
- 用户名：`minioadmin`
- 密码：`minioadmin`
- 用来看/管理上传的反馈图片

---

## 8. 常见问题排查

### ❌ 后端启动报 "Unable to start web server"

**可能原因 1：Docker 服务没启动**
```bash
cd D:/Android/project/ChronoFlow-develop/backend
docker compose up -d
```

**可能原因 2：JWT secret 太短**
检查 `backend/.env` 文件，确保 `JWT_SECRET` 的值至少 32 个字符。

**可能原因 3：数据库连不上**
检查 `backend/.env` 里的 `DB_URL` 是否包含端口 `3307`：
```
DB_URL=jdbc:mysql://localhost:3307/chronoflow?...
```

### ❌ 后端启动时报 "Public Key Retrieval is not allowed"

在 `DB_URL` 中添加 `allowPublicKeyRetrieval=true`（当前配置已包含）。

### ❌ Flutter 编译报 SSL 错误

系统 JDK 17.0.1 的 SSL 证书不全。Flutter 编译需要使用 Android Studio 自带的 JDK 21。

**解决方法**：在 Android Studio 中设置：
1. File → Settings → Build, Execution, Deployment → Build Tools → Gradle
2. Gradle JDK 选择：`D:\Android\Android Studio\jbr`（JDK 21）

### ❌ App 打开后网络请求失败

**模拟器**：确认使用了 `--dart-define=BASE_URL=http://10.0.2.2:8080/api`

**真机**：确认使用了电脑 IP 而不是 `localhost`

**检查后端是否在运行**：浏览器访问 `http://localhost:8080/api/app/version`

### ❌ Docker 报 "port already in use"（端口被占用）

可能是上次的容器没有正常关闭：
```bash
docker compose down
docker compose up -d
```

### ❌ IDEA 找不到 EnvFile 选项

EnvFile 插件没装或没启用：
1. File → Settings → Plugins
2. 搜索 "EnvFile"
3. 安装并重启 IDEA

### ❌ 模拟器启动了但 Flutter 检测不到

```bash
# 查看连接的设备
flutter devices

# 如果没看到模拟器，用 adb 手动连接
adb connect localhost:5555
```

### ❌ 想全部从头来（恢复出厂设置）

```bash
# 1. 删除所有 Docker 数据
cd D:/Android/project/ChronoFlow-develop/backend
docker compose down -v

# 2. 重新启动
docker compose up -d

# 3. 重启后端即可
```

---

## 9. 项目结构速查

```
ChronoFlow-develop/
├── 新手部署指南.md              ← 你正在看的文件
├── README.md                    ← 原始项目说明
├── CLAUDE.md                    ← AI 助手的开发指南
│
├── backend/                     ← 后端（Java）
│   ├── .env                     ← 环境变量配置（数据库密码、密钥等）
│   ├── docker-compose.yml       ← Docker 容器配置
│   ├── init.sql                 ← 数据库建表语句
│   ├── pom.xml                  ← Maven 依赖配置
│   └── src/main/java/.../
│       ├── controller/          ← REST API 接口
│       ├── service/             ← 业务逻辑
│       ├── entity/              ← 数据库实体
│       └── config/              ← 配置类
│
├── frontend/                    ← 前端 App（Flutter）
│   └── lib/
│       ├── main.dart            ← App 入口
│       ├── constants/           ← 常量配置（baseUrl 等）
│       ├── model/               ← 数据模型
│       ├── service/             ← API 调用
│       ├── pages/               ← 各个页面
│       └── widgets/             ← 共享组件
│
└── web/                         ← 管理后台（React）
    └── src/
        ├── api/                 ← API 请求
        ├── components/          ← 组件
        └── pages/               ← 各页面
```

---

## 📋 快速命令速查

```bash
# ===== Docker 服务 =====
cd D:/Android/project/ChronoFlow-develop/backend
docker compose up -d              # 启动 MySQL + Redis + MinIO
docker compose down               # 停止（保留数据）
docker compose down -v            # 停止并删除全部数据
docker ps --filter "name=ChronoFlow"   # 查看服务状态

# ===== 后端 =====
cd D:/Android/project/ChronoFlow-develop/backend
./mvnw spring-boot:run            # 命令行启动后端（需先 export 环境变量）

# ===== 前端 App =====
cd D:/Android/project/ChronoFlow-develop/frontend
flutter pub get                   # 安装依赖
flutter devices                   # 查看连接的设备
flutter emulators                 # 查看可用模拟器
flutter emulators --launch Medium_Phone  # 启动模拟器
flutter run --dart-define=BASE_URL=http://10.0.2.2:8080/api   # 模拟器运行
flutter build apk --debug         # 构建调试 APK

# ===== 管理后台 =====
cd D:/Android/project/ChronoFlow-develop/web
npm install                       # 安装依赖
npm run build                     # 构建（自动输出到后端）
npm run dev                       # 开发模式（热更新）
```

---

## 🌐 所有地址总结

| 地址 | 说明 |
|------|------|
| `http://localhost:8080/api/` | 后端 API |
| `http://localhost:8080/admin/` | 管理后台（admin / admin123）|
| `http://localhost:9001` | MinIO 管理台（minioadmin / minioadmin）|
| `http://localhost:3307` | MySQL 数据库端口 |
| `http://localhost:6379` | Redis 缓存端口 |

---

> 最后更新：2026-07-20
> 如有问题，把这个文档扔给 AI 助手说"按文档帮我排查"即可。
